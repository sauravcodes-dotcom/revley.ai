{
  "name": "@warrant/core",
  "version": "0.1.0",
  "private": true,
  "description": "Pure domain core: effect compiler, invariants, capability authorization. Zero I/O.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "fast-check": "^3.23.1"
  }
}
